
CREATE TABLE info_collector
(
    ID       BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    OWNER    VARCHAR(25) NOT NULL,
    NAME     VARCHAR(15) NOT NULL,
    SURNAME  VARCHAR(50),
    EMAIL    VARCHAR(100),
    NUMBER   BIGINT,
    COUNTRY  VARCHAR(50)
);
